# LI 网站拦截器 · 使用说明

版本 1.0.0 · 简体中文

一个拦截分心网站的浏览器扩展（Chrome / Edge，Manifest V3）：拦截你指定的网站、白名单、随机密码、定期解锁、关键词、多档案、番茄钟、同步备份。

## 安装

### 方式一：GitHub Releases 分发包（推荐）
1. 到本仓库的 **Releases** 页面下载最新 `li-web-interceptor-1.0.0-chrome.zip`；
2. 解压得到一个 `chrome-mv3` 文件夹；
3. 打开 `chrome://extensions`（Edge 为 `edge://extensions`），开启右上角「开发者模式」；
4. 点「加载已解压的扩展程序」，选择解压出的 `chrome-mv3` 文件夹。

### 方式二：本地构建
```bash
npm install
npm run build     # 产物在 dist/chrome-mv3
```
按方式一的第 3、4 步加载 `dist/chrome-mv3`。

> 提示：通过 GitHub Release 安装的用户，后续更新需手动下载新版重新加载（不上架商店就没有自动更新）。

## 快速上手
1. 打开要拦截的网站（如 `www.bilibili.com`）；
2. 点工具栏 🛡️ 图标 →「拦截本站」，页面立即进入拦截页；
3. 在「设置」里可管理拦截列表、白名单、关键词、档案等。

## 功能一览

### 拦截规则
- **匹配模式**：`域名`（默认只拦当前域名，可勾选扩展子域/TLD变体/镜像）、`包含关键字`、`精确域名`、`通配 *`、`全 URL 精确`；
- **拦截类型**：永久 / 计时（N 分钟后自动放行）/ 按次（今日访问满 N 次后拦）/ 排程（按星期+时段）；
- 每条规则可单独开关、可设置重定向 URL。

### 白名单
- 永久放行 / 每日 N 次 / 时段放行；可开启「全站白名单模式」（除白名单外全拦）。

### 拦截页
- 文案或重定向 URL、N 秒自动关闭、倒计时、「定时解锁」（自定义分钟）、随机密码解锁 + 安全问题找回。

### 关键词 / 静默 / 冷却
- 关键词拦截：任意 URL 含关键字即拦；
- 静默拦截：命中后直接关标签页；
- 禁用冷却：关闭总开关后需等待 N 分钟才能重开。

### 档案 / 番茄钟 / 历史 / 统计
- 多档案一键切换场景；
- 番茄钟（专注/休息、角标倒计时、系统提醒）；
- 历史记录与今日统计（拦截数、Top 网站、番茄数）。

### 同步备份
- 「设置 → 同步」可配置 **WebDAV 或 S3** 存储，上传/下载数据快照（凭据本地加密保存）。

## 快捷操作
- **地址栏**：输入 `bl ` 后回车 → `bl add 域名` 加拦截、`bl allow 域名` 加白名单、`bl on/off` 开关、`bl list` 打开设置；
- **快捷键**：`Ctrl/Cmd+Shift+L` 开关拦截、`Ctrl/Cmd+Shift+B` 拦截当前网站（可在 `chrome://extensions/shortcuts` 修改）；
- **弹窗**：拦截本站、加入白名单、全站白名单、暂停 10 分钟、番茄钟。

## 常见问题（FAQ）
**Q：拦截了网站但还能访问？**
A：请确认弹窗顶部「启用拦截」开关是开启的；若处于「暂停」状态请点恢复。

**Q：数据会丢失吗？**
A：数据存在本机浏览器。建议定期「设置 → 数据 → 导出 JSON」备份，或配置同步。

**Q：卸载/禁用扩展后还会拦截吗？**
A：不会。本扩展为软性自律，卸载/禁用即失效（浏览器对普通扩展的限制）。

**Q：如何换语言/主题？**
A：设置 → 设置 Tab → 外观：主题（跟随系统/浅/深）、语言（跟随系统/中文/English）。

## 已知限制
- 无法阻止用户卸载/禁用扩展（浏览器限制，软性自律）；
- 通过 GitHub Release 安装无自动更新，需手动更新。

## 隐私
见 [PRIVACY.md](./PRIVACY.md)。
